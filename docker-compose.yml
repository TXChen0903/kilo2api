services:
  kilo2api:
    build: .
    container_name: kilo2api
    ports:
      - "10904:9090"
    volumes:
      - ./data:/app/data
    environment:
      - HOST=0.0.0.0
      - PORT=9090
    restart: unless-stopped
